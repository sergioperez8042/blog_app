# Production Environment Variables
NODE_ENV=production

# Para producción, usar MongoDB Atlas o una URL de producción
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/blog_app?retryWrites=true&w=majority

# Temporalmente, usar modo sin DB para evitar errores
MONGODB_URI=
